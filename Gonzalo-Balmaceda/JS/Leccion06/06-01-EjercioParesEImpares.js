// Encontrar números paras e impares.
const numero = 10;
for (let i = 1; i <= numero; i++) {
    if (i % 2 === 0) {
        console.log(`pares = ${i}`);
    } else {
        console.log(`impares = ${i}`);
    }
}

